# Biomedical-Field-Service-engineer
VLD-RAG-inspired approach
basically a multimodal RAG approach that doesn’t treat a PDF as just plain text. It keeps the text, images, tables, and page structure, then uses both text and visual retrieval to find the most relevant pages before sending them to the VLM/LLM to generate the answer.
